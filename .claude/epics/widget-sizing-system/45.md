---
name: Etendre les registries widgets (etudiant + prof)
status: open
created: 2026-03-28T19:43:53Z
updated: 2026-03-28T19:47:21Z
github: https://github.com/rohanfosse/cursus/issues/45
depends_on: [001]
parallel: true
conflicts_with: []
---

# Task: Etendre les registries widgets (etudiant + prof)

## Description

Migrer les registries existants (`student-widgets/registry.ts` et `useTeacherBento.ts`) vers le nouveau format `WidgetDef` avec tailles, categories et roles.

## Acceptance Criteria

- [ ] `STUDENT_WIDGETS` dans `registry.ts` utilise le nouveau `WidgetDef` avec `sizes`, `defaultSize`, `category`
- [ ] `TEACHER_TILES` dans `useTeacherBento.ts` migre vers `WidgetDef` (remplace `TeacherTileDef`)
- [ ] Chaque widget a des tailles realistes (ex: clock=1x1 only, project=1x1/2x1/2x2)
- [ ] Categories assignees a chaque widget
- [ ] Retrocompatibilite : les composants existants continuent de fonctionner

## Technical Details

Tailles par widget etudiant :
- `clock` : [1x1] — `quote` : [1x1, 2x1] — `countdown` : [1x1]
- `project` : [1x1, 2x1, 2x2] — `exams` : [1x1, 2x1] — `livrables` : [1x1, 2x1]
- `soutenances` : [1x1, 2x1] — `feedback` : [1x1, 2x1] — `grades` : [1x1, 2x1, 2x2]
- `progress` : [1x1] — `calendar` : [2x1, 2x2] — `bookmarks` : [1x1, 2x1]
- `promoActivity` : [2x1, 4x1] — `live` : [2x1, 4x1]

Fichiers : `src/renderer/src/components/dashboard/student-widgets/registry.ts`, `src/renderer/src/composables/useTeacherBento.ts`

## Dependencies

Task 001 (types)

## Effort Estimate

- Size: S
- Hours: 1

## Definition of Done

- [ ] Code implemented
- [ ] TypeScript passe
- [ ] Widgets existants non impactes
