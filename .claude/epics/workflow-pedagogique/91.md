---
name: Migration DB + model scheduled_publish_at
status: open
created: 2026-03-30T21:02:15Z
updated: 2026-03-30T21:09:31Z
github: https://github.com/rohanfosse/cursus/issues/91
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Migration DB + model scheduled_publish_at

## Description

Ajouter la colonne `scheduled_publish_at` (TEXT, nullable, ISO datetime) a la table `travaux` dans le schema SQLite. Mettre a jour le model `assignments.js` pour lire/ecrire ce champ. Exposer via IPC dans preload.

## Acceptance Criteria
- [ ] Colonne `scheduled_publish_at TEXT` ajoutee a la table `travaux` via migration
- [ ] `PRAGMA user_version` incremente
- [ ] Model `createTravail` et `updateTravail` gere le nouveau champ
- [ ] IPC expose `updateTravailScheduled({ travailId, scheduledAt })`
- [ ] Type TypeScript `Devoir` mis a jour avec `scheduled_publish_at?: string | null`
- [ ] Migration backward-compatible (tryAlter pattern existant)

## Technical Details

- Fichier schema : `server/db/schema.js` (pattern `tryAlter`)
- Model : `server/db/models/assignments.js`
- IPC : `src/ipc.js` + `src/preload/index.ts`
- Types : `src/renderer/src/types/index.ts` (interface Devoir)

## Dependencies

Aucune -- tache fondation.

## Effort Estimate
- Size: S
- Hours: 1-2
